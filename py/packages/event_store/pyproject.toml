[project]
name = "event-store"
version = "0.1.0"
description = "Abstract event store interface for persisting and retrieving events"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/event_store"]
